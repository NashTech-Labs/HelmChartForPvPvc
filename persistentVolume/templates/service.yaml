apiVersion: v1
kind: Service
metadata:
  name: {{ include "persistentVolume.fullname" . }}
  labels:
    {{- include "persistentVolume.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "persistentVolume.selectorLabels" . | nindent 4 }}
